```sh
adrus /some/note               # open note for editing  
adrus /some/note +tag -tag     # modify note's tags
adrus ? +tag -tag              # query notes

adrus rm /some/note            # delete note
adrus ar archive.tar +foo -bar # archive notes matching query
```
